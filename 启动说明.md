# RM26 启动说明（remote / mouse）

本文档结合 `rm_if_chassis_control/README.md` 与 `rm26_auto_aim/README.md` 整理，遵循以下原则：
- 有 `launch` 脚本时优先使用 `launch`；
- 无 `launch` 脚本时使用 `ros2 run`；
- 明确区分 `remote` 与 `mouse` 两种控制模式。

## 1. 启动前准备

```bash
cd ~/RMUL2026/rm26_ros2_ws
source /opt/ros/foxy/setup.bash
./setup.sh
colcon build --symlink-install
source install/setup.bash
```

说明：
- `setup.sh` 会配置 `can0/can1/can2` 和 `ttyUSB0/ttyUSB1` 权限；
- 若已完成编译，可跳过 `colcon build`。

---

## 2. 各功能包启动方式总览

### 2.1 有 launch 的功能包

- `rm_if_chassis_control`
  - `ros2 launch rm_if_chassis_control chassis.launch.py`
- `rm26_auto_aim`
  - `ros2 launch rm26_auto_aim auto_aim.launch.py`

### 2.2 无 launch 的功能包（直接启动节点）

- `vt_remote_control`
  - `ros2 run vt_remote_control vt_remote_node --ros-args -p serial_port:=/dev/ttyUSB1 -p baud_rate:=921600`
- `rm_gimbal_control`
  - `ros2 run rm_gimbal_control gimbal_control_node`
- `ammo_booster_control`
  - `ros2 run ammo_booster_control ammo_booster_node --ros-args -p can_interface:=can2 -p fire_control_source:=<remote|mouse>`
- `rm_serial_cm`（占位串口节点）
  - `ros2 run rm_serial_cm serial_cm_node`

---

## 3. mode 开关说明（节点运行时动态切换）

所有相关节点（底盘/云台上层/发射）现在统一使用 `/vt_remote/switches` 的 `mode` 值决定输入源：

- `mode=0`：使用 `mouse` 控制
- `mode=1`：使用 `remote` 控制

> 建议在实际操作前先确认图传遥控器当前 `mode` 档位；无需再通过启动参数强制指定输入源。

---

## 4. remote 模式（遥控器主控，默认包含自瞄）

### 3.1 推荐启动命令

```bash
# 1) 图传遥控器输入（无 launch）
ros2 run vt_remote_control vt_remote_node --ros-args \
  -p serial_port:=/dev/ttyUSB0 \
  -p baud_rate:=921600

# 2) 云台控制（无 launch）
ros2 run rm_gimbal_control gimbal_control_node

# 3) 底盘（有 launch，推荐）
ros2 launch rm_if_chassis_control chassis.launch.py \
  serial_port:=/dev/ttyUSB1 \
  baud_rate:=460800 \

# 4) 发射（无 launch）
ros2 run ammo_booster_control ammo_booster_node --ros-args \
  -p can_interface:=can2

# 5) 自瞄（有 launch，默认必须启动）
ros2 launch rm26_auto_aim auto_aim.launch.py
```

---

## 5. mouse 模式（键鼠主控，默认包含自瞄）

`mouse` 模式下，核心是将遥控器 `mode` 置为 `0`。

### 4.1 推荐启动命令

```bash
# 1) 图传遥控器输入（无 launch）
ros2 run vt_remote_control vt_remote_node --ros-args \
  -p serial_port:=/dev/ttyUSB0 \
  -p baud_rate:=921600

# 2) 云台控制（无 launch）
ros2 run rm_gimbal_control gimbal_control_node

# 3) 底盘（有 launch，直接切 mouse 模式）
ros2 launch rm_if_chassis_control chassis.launch.py \
  serial_port:=/dev/ttyUSB1 \
  baud_rate:=460800

# 4) 发射（无 launch）
ros2 run ammo_booster_control ammo_booster_node --ros-args \
  -p can_interface:=can2

# 5) 自瞄（有 launch，默认必须启动）
ros2 launch rm26_auto_aim auto_aim.launch.py 
```

---

## 6. 关键默认参数说明（来自包内 README/launch）

- `rm_if_chassis_control/chassis.launch.py`
  - 新增 launch 参数：`serial_port`（默认 `/dev/ttyUSB1`）、`baud_rate`（默认 `460800`）
- `rm26_auto_aim/auto_aim.launch.py`
  - 新增 launch 参数：`engine_path`（默认工程内 `.engine` 路径）
  - 同时启动 `hk_camera_node`、`armor_detector_node`、`auto_aim_node`
- `ammo_booster_control/ammo_booster_node`
  - 保留参数：`fire_control_source`、`input_priority_timeout`（建议仅作为 `mode` 非 0/1 时的兜底）

说明：
- 控制输入源以 `/vt_remote/switches` 的 `mode` 为准（`0=mouse`，`1=remote`）。
- `gimbal_control_source` / `fire_control_source` 等参数建议仅保留为兼容兜底，不再作为主切换方式。

---

## 7. 建议启动顺序

1. `./setup.sh`
2. `vt_remote_node`
3. `gimbal_control_node`
4. 底盘（`chassis.launch.py`，按模式传 `control_mode`）
5. `ammo_booster_node`
6. 自瞄链路（`auto_aim.launch.py`，两种模式都默认启动）

---

## 8. 快速自检

```bash
ros2 node list
ros2 topic list
ros2 topic echo /vt_remote/channels
ros2 topic echo /vt_remote/mouse
ros2 topic hz /detector/armors
```

